# CLAUDE.md

This file gives Claude Code context on this project. Keep it updated as decisions change — this is a living document, not a one-time spec. **This file is a condensed, current-state summary — the full page-by-page build history (what was wired, what was verified, exact gotchas hit) lives in [`docs/build-log.md`](./docs/build-log.md), append-only. Add new entries there, not here; only update this file's summary sections when the *current state* actually changes.**

**System design decisions are made together with the user, not unilaterally.** When a task requires a new architectural choice (a new service, a new data model shape, a new provider), surface it as a decision rather than picking silently — even if there's an obvious-seeming default.

## Project Overview

**TYP** is a Ghana-focused exam-prep and digital skills platform. BECE (Junior High School exit exam) is the flagship track, but the platform is scoped to cover multiple exam/skill tracks: **BECE, WASSCE, nursing entrance/licensing exams, university entrance exams, and digital skills training** (modeled via a top-level `Program` entity, `Subject.programId` FK — only BECE has real content authored so far). Schools subscribe to give their students access to practice assessments; individual ("independent") students can also self-subscribe. Content is produced by content admins, vetted by a super admin, then made available as timed assessments students take online with automatic scoring, results, progress tracking, and leaderboards.

**The product is two deliberately separate systems** (decided 2026-08-05):
1. **The exam-prep track** — internal Content Admins, super-admin pre-approval, `Program`/`Subject`/`Question`/`Assessment`/`ExamAttempt`. Fully built and verified against the live DB (see Current State below).
2. **A course marketplace**, Coursera/Udemy-style — independent **Tutors** self-onboard and publish real courses (video lessons, reading material, structured modules) directly to students. Locked decisions: **publish-first, moderate-after** (not pre-approved like exam-prep); **a la carte per-course pricing** with a platform cut (currently 15%, super-admin-editable via `PlatformSettings`) rather than pooled subscription revenue-share; **per-course enrollment** (`Course → Enrollment → Student`), no fixed tutor roster; **Content Admin and Tutor are distinct roles**, not a merge. Video hosting is an external URL the tutor pastes in (not Vercel Blob); lesson-completion progress tracking is out of scope for now; tutor payout uses Paystack's native Split Payments (subaccounts) — a tutor connects a real bank account via `app/tutor/settings`'s Payouts tab (built 2026-08-08), which creates a real Paystack subaccount and stores its code on `TutorProfile.paystackSubaccountCode`; course purchases route the platform's exact cut there automatically once connected, full charge to the platform's main account otherwise.
   These two systems share only `User` (base identity/auth) and `Student` identity — `Course` is not an `Assessment`.

**Current state (updated 2026-08-08)**: frontend + a real, seeded Neon database + real Auth.js auth. **All 4 exam-prep role lanes are fully wired and verified against the live DB** (Content Admin, School Admin, Super Admin, Student + public/signup — every page, including all 5 dashboard-root pages found by a later full-folder audit). The full core exam-taking loop is real end-to-end: content created → approved → assigned → student sees it → takes it (server-graded, anti-cheat tab-switch logging) → real result. Account-creation loops (school self-register, student self-register via independent signup or school-code join) are real. **Paystack checkout is real plumbing** for school subscriptions, independent-student subscriptions, and course purchases — webhook-verified end-to-end via hand-signed test payloads, idempotent, PCI-safe (no raw card entry anywhere), with tutor payout Split Payments now wired end-to-end code-wise too. **Decided/built 2026-08-14**: real Paystack test keys, a real Resend key on a verified sending domain, and a real Sentry auth token/DSN are now in `.env`, independently verified live (not just present) — see that date's `docs/build-log.md` entry for exact verification detail. A live subaccount creation still hasn't been exercised. **Decided/built/verified 2026-08-14**: the app's first real deployment now exists at `https://testingyourpreparedness.com` — Paystack/Resend/Sentry/Upstash keys are in Vercel's project environment variables and a real login round-trip against the live DB has been confirmed (see Tech Stack section below for the build fix and verification detail). **The Tutor role has a first full vertical slice**: self-signup, dashboard, course authoring (create/extend), payout account setup, a student-facing catalog/purchase/lesson-viewer flow, and super-admin moderation (`super-admin/courses` flag/remove, `super-admin/tutors` roster + suspend). **Everything above is now committed to git** (2026-08-08, grouped into ~10 logical commits by lane/feature — schema+migrations, dependencies, each role lane, Paystack infra, the course marketplace, and this doc restructuring — see `git log` for the exact breakdown; nothing was squashed into one giant commit). The frontend's shape — what fields forms collect, what statuses things move through, what each role can see and do — **is the spec for the backend.**

**What's left**: a live Paystack subaccount creation, a live scheduled-report email, and a live end-to-end Paystack payment on the real deployed domain haven't been exercised yet (real keys now exist as of 2026-08-14, and the app itself is now deployed too, but none of these flows has been run against the live site yet).

**Decided 2026-08-08**: rate-limiting backend is **Upstash Redis** (`@upstash/ratelimit`) — confirmed with the user first. In-memory rate limiting doesn't work on Vercel's serverless model (no shared state across instances), and reusing Postgres for request counters was explicitly passed over in favor of a purpose-built tool for this exact access pattern.

**Decided/built 2026-08-08**: rate limiting is real on `login`, `signup/school`, `signup/independent`, `signup/tutor`, `join`, `forgot-password`, and `contact` — `lib/rate-limit.ts` wraps `@upstash/ratelimit`+`@upstash/redis` (internal-interface pattern, same as `lib/payments/paystack.ts`). **Fails open** when unconfigured or on a real Upstash error (deliberate: rate limiting is defense-in-depth on top of already-real validation, not the security boundary itself — fail-closed would mean a Redis outage silently takes down login/signup platform-wide, a worse failure mode). `NextRequest.ip` is confirmed removed in this Next.js version (per this file's own standing warning) — IP comes from `x-forwarded-for`/`x-real-ip` headers instead. See `docs/build-log.md`'s 2026-08-08 entry.

**⚠️ Decided/built 2026-08-08 — security audit found and fixed a real, exploitable `passwordHash` leak across 14 pages.** The most severe finding this project has had. Any authenticated super admin (and, on 2 pages, any school admin) loading an existing page could read other real users' actual bcrypt password hashes straight out of the page's HTML — `include: { user: true }` (or `createdBy`/`reviewedBy`/`actor: true`) fetched the full `User` row and it flowed unmapped into a client component's props. Fixed on all 14 sites (`super-admin/courses/[id]`, `super-admin/schools/[id]`, `super-admin/tutors/[id]`, `school-admin/students`, `school-admin/assessments/assign`, `super-admin/tutors`, `super-admin/content-admins`, `super-admin/courses`, `super-admin/payments`, `content-admin/assessments`, `super-admin/question-bank`, `content-admin/questions/pending`, `super-admin/page.tsx`, `super-admin/audit-logs` — plus `super-admin/revenue` tightened as a precaution, not an actual leak) using `select` at the query level where possible (Prisma never fetches the column at all) or destructuring `passwordHash` out after fetch, matching the pattern `school-admin/settings` already got right once before. **Also fixed**: `initializeSchoolCheckout`/`initializeStudentCheckout` (`signup/school`, `signup/independent`) never verified the caller owned the `schoolId`/`studentId` passed in — a real defense-in-depth gap (not exploitable for payment fraud, since the Paystack webhook remains the sole source of truth), now guarded. **Also found, deliberately not fixed**: nearly every `super-admin/**` Server Component has no `auth()` call of its own, relying entirely on `proxy.ts`'s route-level check — currently unexploitable, flagged for a future pass rather than touching ~15 files for redundant protection right now. Tenant-scoping (a separate audit category) came back completely clean. Full writeup, every fix, and every live-DB verification in `docs/build-log.md`'s 2026-08-08 entry — read it before touching any of these pages again.

**Decided/built 2026-08-08** — `auth()` guards added to every `app/super-admin/**` page, closing a deferred security-audit finding: every page now checks the visitor's role itself rather than relying solely on `proxy.ts`. Verified live, typecheck clean. See `docs/build-log.md`'s 2026-08-08 entry.

**Decided/built 2026-08-08, upgraded 2026-08-10** — `/terms` and `/privacy` originally shipped as clearly-labeled placeholders (confirmed with the user), then upgraded to real, product-accurate drafted legal text — still explicitly a first draft pending legal review, not final. See `docs/build-log.md`'s 2026-08-10 entry.

**Decided/built 2026-08-08 — a follow-up broken-link audit found 7 more dead links, all in the shared public header/footer**: `/features`, `/about`, `/help` now have real "still being built" placeholder pages (new shared `components/coming-soon-notice.tsx`); `/cookies` got the same legal-placeholder treatment as `/terms`/`/privacy` (new shared `components/legal-placeholder-notice.tsx`, and `/terms`/`/privacy` refactored to use it too); `/blog` and the `/schools`/`/students` footer links were removed outright rather than stubbed (discussed with the user — no real intent behind the latter two, and a contentless "coming soon" blog reads worse than no blog link). See `docs/build-log.md`'s 2026-08-08 entry.

**Decided/built 2026-08-08 — dead/no-op UI elements audit found and fixed 4 real issues.** `school-admin/students`'s row-actions dropdown (View Profile/Edit/Send Message) was 3 dead menu items with nowhere real to go — removed entirely, same precedent as `content-admins`' own dropped items. Its "Export" button was a genuine no-op while sibling pages (leaderboard/results) have working ones — fixed, moved into the client-component table since a Server Component button can't have a real `onClick`. `super-admin/schools`'s "View Details"/"Edit" were dead, and worse — the real, already-built `schools/[id]` detail page was completely unreachable from the UI at all. "View Details" now links there for real (school name cell too); "Edit" dropped, no edit flow exists. The "Add School" dialog had a fully fake form (uncontrolled inputs, submit does nothing but close) — replaced entirely with a direct link to the real `/signup/school` flow rather than dressing up the fake one. See `docs/build-log.md`'s 2026-08-08 entry.

**Decided/built 2026-08-08**: N+1/expensive-query audit found and fixed one real N+1 (`student/results/page.tsx` + `student/page.tsx` both called a per-attempt rank query inside a loop — rewritten to one batched query each) plus one genuinely missing index it directly benefits from (`ExamAttempt` had no index on `assessmentId` at all — added). Verified live, including a real query-count reduction demo (5 sequential round trips → 1). See `docs/build-log.md`'s 2026-08-08 entry.

**Decided/built 2026-08-08 — mobile/responsive audit, public/marketing + signup/auth half (static-only, no browser tooling available — confirmed with the user before starting).** One real, clear finding: `app/join/page.tsx`'s First/Last Name field grid had no responsive prefix at all (`grid-cols-2` unconditionally) while every sibling signup form uses `sm:grid-cols-2` — fixed. Everything else in scope checked out clean (no tables, no other ungated grids, no oversized fixed-width dialogs). See `docs/build-log.md`'s 2026-08-08 entry for exactly what was and wasn't verifiable without a browser.

**Decided/built 2026-08-08 — mobile/responsive audit, authenticated-dashboards half (static-only, same caveat as the public-pages half above).** Scoped to `app/super-admin/**`, `app/school-admin/**`, `app/content-admin/**`, `app/student/**`, `app/tutor/**`. Mostly a clean result once shared-component defaults were checked (`Table`'s wrapper already handles horizontal scroll everywhere, `DialogContent` already caps mobile width by default — invalidated ~25 grep candidates as false positives). 4 real findings, all the same mistake: a fixed-width search Input (or Input+Select pair, or chart+legend pair) sharing a non-wrapping `flex` row, unlike the "Filters:" toolbars elsewhere in this codebase that already correctly use `flex-wrap` — fixed in `super-admin/schools/[id]`, `school-admin/classes`, `content-admin/assessments/create`, and `content-admin`'s own dashboard chart+legend layout. Two candidates investigated and confirmed correct-as-is, not bugs (a 7-column day-of-week heatmap, a 4-column tabs list with an existing icon-only mobile fallback). See `docs/build-log.md`'s 2026-08-08 entry for the full per-file breakdown.

**Decided/built 2026-08-08**: `Invitation` now has a real acceptance flow — a school-admin invite email carries a genuine one-click accept link (`app/invite/accept`, 7-day-expiry token), not just a pointer to the general signup page. `lib/demo-data.ts` (the original pre-wiring-push demo file, confirmed unused) was also deleted. See `docs/build-log.md`'s 2026-08-08 entries.

**Decided/built 2026-08-08**: guardian approval is real — `Guardian.approvedAt` is no longer set from a self-attestation checkbox; it now only gets set when the guardian clicks a real emailed link (`app/guardian/approve`, same token/expiry pattern as `Invitation`). Guardian email is now **required** on independent-student signup (confirmed with the user — a real approval flow can't function without an address). See `docs/build-log.md`'s 2026-08-08 entry.

**⚠️ Reversed 2026-08-10**: the Guardian step was removed from independent-student signup entirely (confirmed with the user, who was told this breaks the guardian-approval-email requirement above before confirming). `app/signup/independent/independent-signup-wizard.tsx` no longer collects guardian name/phone/email and is down to 3 steps (Personal Info, Location, Plan); `registerIndependentStudent` in `app/signup/independent/actions.ts` no longer creates a `Guardian` row or sends the approval email. No schema change was needed since `Student.guardian` was already an optional relation. `app/guardian/approve/**` (the approval-link flow itself) was deliberately left in place — it's now unreachable for new signups but untouched in case it's needed again; not deleted since removing it wasn't asked for.

**Decided/built 2026-08-08**: email provider is **Resend** — confirmed with the user first. `lib/email/` now provides real send infrastructure, exercised for real by the Invitation-accept and guardian-approval flows below. **The broader rollout is also done**: password-reset links, school-admin invites (plus a resend action), new-account temp passwords for content admins and school-added students (plus resend actions — a fresh temp password is generated since only the bcrypt hash was ever persisted, never the plaintext), and assessment-assignment notifications (sent to the resolved set of direct + class-targeted students, sequentially — bounded per-school scale, not routed through the background-jobs question below). All sends are best-effort (`sendEmailBestEffort`) so a down/unconfigured provider never blocks the underlying action. Bulk imports (`content-admin/questions/upload`, `school-admin/students/add`) also got hard row caps (300/200) as part of the same pass, closing the Background Jobs decision's synchronous-with-a-cap approach. See `docs/build-log.md`. **Fixed 2026-08-15**: a real, previously-missing welcome email now fires on all 4 self-signup paths (school admin, independent student, tutor, school-code join) — a genuine gap until then (nobody self-signing up ever got any confirmation email at all), found when the user created a real tutor account and got silence.

**Decided/built 2026-08-08**: a real `ContactMessage` inbox and the scheduled weekly platform report both landed, closing the remaining half of the Reports/Export open decision. `app/super-admin/messages` lists real contact-form submissions (mark-as-read action, real unread-count nav badge, same "actionable queue" philosophy as Review Queue/Payments) — previously `ContactMessage` rows were written by the public contact form but had no admin-facing view at all. `app/api/cron/weekly-reports` (scheduled via `vercel.json`'s native Vercel Cron, checked against the Background Jobs decision's "no new vendor" call) emails every super admin the same 3 datasets `super-admin/reports` already shows, as both XLSX and PDF attachments, via a shared `lib/reports/generate.ts` (the page and the cron route now call the same function rather than duplicating the aggregation logic) — verifies `CRON_SECRET` when set, logs each run to `AuditLog` (`actorId: null` for a system-triggered run). Still needs a real Vercel Cron deployment + Resend key to exercise a live run, same standing caveat as the rest of this list.

**Decided/built 2026-08-08**: error monitoring is **Sentry** (`@sentry/nextjs`) — `instrumentation.ts`/`instrumentation-client.ts` (this Next.js version's real instrumentation convention, confirmed against its own bundled docs) load `sentry.server.config.ts`/`sentry.edge.config.ts`/the client init, all gated on `NEXT_PUBLIC_SENTRY_DSN` — same "no client built at all when unconfigured" pattern as Paystack/Resend/Upstash, so it's safe to ship with no real DSN yet. `next.config.mjs` wraps the config in `withSentryConfig` for source-map upload (degrades gracefully without `SENTRY_AUTH_TOKEN`, verified against a real build) and is already pointed at a real org/project (`akrynix-labs`/`javascript-nextjs`). Also added: `app/global-error.tsx` (captures unhandled render errors) and the wizard-generated `app/sentry-example-page`/`app/api/sentry-example-api` (a self-contained test route for verifying a real DSN once one exists — harmless either way since the underlying client is a no-op without one). Still needs a real DSN to actually receive anything.

**Decided/built 2026-08-10**: package manager is pinned to **npm** (`package.json`'s `packageManager` field) — a `pnpm-lock.yaml`/`pnpm-workspace.yaml` pair had appeared in the working tree (from a Sentry-wizard run that defaulted to pnpm; `node_modules` itself was still npm-managed, so no real pnpm install had happened) alongside the existing `package-lock.json`. Two lockfiles risk Vercel or a future contributor picking the wrong installer. Removed the pnpm artifacts, kept `package-lock.json` (regenerated once, resolved pre-existing lockfile drift on a few optional platform-specific packages, no dependency changes). npm was the already-established choice everywhere else in this project (every script/doc references `npm run ...`); switching to pnpm was never a decision anyone made.

**Decided/built 2026-08-08**: course reviews/ratings are real — a new `CourseReview` model (upsert-able, gated on real `Enrollment`), wired into the student catalog, the student course detail page (review list + submit/edit form), and a bonus average-rating stat on the tutor's own course detail page. See `docs/build-log.md`'s 2026-08-08 entry.

**Decided/built 2026-08-08**: `content-admin/questions/[id]/edit` is real — mirrors `questions/create`'s field shape, scoped to the creator (same check as `deleteQuestion`), editable only while `draft`/`rejected` (editing a question mid-review or already-live in an assessment needs its own re-review/versioning workflow — flagged as a real open question, not built). Both previously-dead "Edit"/"Revise & Resubmit" stubs now wired to it. See `docs/build-log.md`'s 2026-08-08 entry.

**Decided/built 2026-08-08**: a manual browser walkthrough (the first this whole push had — everything before was verified via scripts/RSC-payload greps) caught 7 leftover fake nav-badge numbers across 4 role shells (e.g. Schools showing hardcoded `"127"`, Students `"45.6K"`) — invisible to the earlier `lib/demo-data.ts` dead-code audit since they were inline literals, not imports. Fixed and expanded: **16 nav badges across all 5 role shells now show real, live Prisma counts**, each reusing the exact definition its destination page already computes (e.g. `overdueInvoices` not total invoices, `examsInProgress` not a static count) — badges were added only to rosters/actionable-queue items, deliberately skipped on action links and pure-reports pages to avoid noise. See `docs/build-log.md`'s 2026-08-08 entry for the full per-role breakdown and the one flagged approximation (`student/layout.tsx`'s "Available Exams" badge skips the full per-assignment attempt-limit check for cost reasons, documented inline).

**Decided/built 2026-08-08**: suspending a tutor (`super-admin/tutors`) **does** cascade — sets all their `published` courses to `flagged` (not `removed`), consistent with the existing moderation lever and reversible on reactivate (an AuditLog-based check means reactivating only un-flags courses the cascade itself flagged, never a course a moderator separately flagged for an unrelated reason). Also added a payout-account disconnect/reconfigure action to `app/tutor/settings`'s Payouts tab. Both done and verified — see `docs/build-log.md`'s 2026-08-08 entry.

**Decided/built 2026-08-08**: `super-admin/reports` now exports all 3 real datasets as PDF and Excel too, not just CSV. Excel reused the already-present `xlsx` dependency (no new package); PDF added `jspdf`+`jspdf-autotable` (both flagged before install, zero new vulnerabilities). Done and verified — see `docs/build-log.md`'s 2026-08-08 entry. Scheduled/recurring report generation is the only piece of the original export item still open, and it stays blocked on the Background Jobs decision below.

Full page-by-page build history — every wiring decision, every "honest reshaping" call, every verification method, every gotcha — is in **[`docs/build-log.md`](./docs/build-log.md)**. Read it when you need to know *why* a specific page looks the way it does or *how* something was verified; don't re-derive it from scratch.

## Roles & Access Model

Five roles exist: 4 in the frontend from the start, plus **Tutor** (added 2026-08-07 for the course marketplace, has its own layout/nav at `/tutor`).

| Role | Dashboard root | Core responsibilities |
|---|---|---|
| **Super Admin** | `/super-admin` | Approves/rejects questions & assessments; manages schools, content admins, tutors, subjects/topics, subscription plans, revenue/payments, platform-wide analytics, audit logs, course moderation |
| **Content Admin** | `/content-admin` | Creates questions (single-entry or bulk CSV/XLSX) and assessments; everything enters a review queue — nothing goes live without super admin approval |
| **School Admin** | `/school-admin` | Registers/manages a school; adds students and classes/forms; assigns assessments to classes; views results/leaderboard; manages the school's subscription and billing |
| **Student** | `/student` | Takes timed assessments, views results/progress/leaderboard/study materials, browses/buys/watches marketplace courses. Two flavors: **school-provisioned** (no personal billing) and **independent** (self-registers, personal subscription, guardian info required) |
| **Tutor** | `/tutor` | Self-service signup (no approval gate), authors courses (modules/lessons), views enrolled students and real earnings (`tutorPayout` after the platform cut) |

Auth is real Auth.js (Credentials provider + JWT sessions, no adapter — own Postgres `User` table), role-based route protection enforced server-side in `proxy.ts` (this Next.js version's `middleware.ts` equivalent).

**Decided/built 2026-08-17 — real email/account verification, confirmed with the user ahead of a real 3-week launch date.** Scope confirmed: self-signup only (school registration, independent student, tutor signup, school-code join), blocks login until verified (not a soft nudge) — matches the precedent already set by the school-join-approval and guardian-approval gates. Admin-provisioned accounts (content admin, school-added student, an accepted `Invitation`) are deliberately exempt — each already proves email ownership by the recipient receiving/using a real emailed temp password or invite link, so a second gate would be redundant. `User` gained `emailVerified Boolean @default(true)` (default `true`, not `false` — this is a live table with real existing rows; a `false` default would have retroactively locked every current user out on migration, verified live: `26` existing users, `0` unverified after the migration ran) plus `emailVerificationToken`/`emailVerificationTokenExpiresAt` (same shape as the existing `resetToken` pair). New `lib/email-verification.ts` (`sendVerificationEmailBestEffort`/`verifyEmailToken`/`resendVerificationEmailBestEffort`, same internal-interface/best-effort pattern as every other email flow here) wired into all 4 self-signup creation sites, each explicitly passing `emailVerified: false`. `auth.ts` gained a new `EmailNotVerifiedError` (`code: "email_not_verified"`), checked right after password match, before the existing student-pending-approval check. `app/verify-email` (new) requires a real button click to confirm — deliberately not a bare-GET-mutates-on-load page, since some email clients/security scanners pre-fetch links in an inbox before a user opens the email, which would silently burn a one-time token before the real recipient ever clicked it (same reasoning `app/invite/accept` already followed). `app/login/page.tsx` surfaces the new error with a "Resend verification email" action. **A real, load-bearing side effect discovered while building this**: `initializeStudentCheckout` (`app/signup/independent/actions.ts`) required an authenticated session, which the independent-student wizard could no longer get immediately post-signup (no auto-sign-in anymore, since it would just fail against an unverified account) — fixed by extending it to accept an unauthenticated call when the target student genuinely has no existing `Subscription` yet, the exact same allowance `initializeSchoolCheckout` already had (not a new security loosening, just applying an already-audited pattern consistently). All 3 self-signup wizards (school, independent student, tutor) no longer attempt `signIn()` right after registration — they show a real "check your email" confirmation screen instead, since that attempt would now always fail. Mobile: `app/api/mobile/auth/login`'s route gained the matching `email_not_verified` 403, a new `POST /api/mobile/auth/resend-verification` route (no auth required, since a blocked user has no token yet), `LoginScreen` shows the same resend affordance, and `JoinSchoolScreen`'s pending-approval confirmation copy now mentions both gates (email verification AND school-admin approval) since school-code join needs both. Live-verified end to end against the real Neon DB with a temp script (create-unverified → verify-with-valid-token → confirm-cleared → verify-with-expired-token-rejected → cleanup), not just typechecked. Backend `npx tsc --noEmit --skipLibCheck` and mobile `flutter analyze`/`flutter test` all clean.

## Core Domain Entities

Short summary — see [`docs/data-model.md`](./docs/data-model.md) for the full field-by-field version, and [`docs/build-log.md`](./docs/build-log.md) for the reasoning behind each shape.

- **User** — base identity; `role` enum (`super_admin | content_admin | school_admin | student | tutor`); email/password. **Decided/built 2026-08-15**: real self-service account deletion for student/tutor (`deletionRequestedAt`/`scheduledDeletionAt`/`deletedAt`, all nullable) — a 30-day grace period, cancellable, with 3 new emails. Deliberately **anonymizes, not hard-deletes** — `Course.tutorId`/`Enrollment.studentId` are `ON DELETE RESTRICT` in the live schema, so a real hard delete would either fail for anyone with real history or require loosening those constraints, which has real consequences for other people's data (an enrolled student's course disappearing because their tutor deleted their account) — confirmed with the user rather than decided unilaterally. See `lib/account-deletion.ts` and `docs/build-log.md`'s 2026-08-15 entry.
- **School** — name, GES reg. number, region/district/town/address, contact, `status` (`active|pending|suspended`), `ownershipType` + `educationLevel` (two separate fields).
- **Student** — belongs to a `School` (nullable for independent students), class/form; independent students carry a `Guardian` relation + subscription.
- **Class/Form** — canonical naming "Form N[A/B/C]"; identity is `(schoolId, form, section, academicYear)`, enforced by a compound unique constraint.
- **Program** — BECE, WASSCE, Nursing, University Entrance, Digital Skills. `Subject.programId` FK.
- **Subject** / **Topic** — `Topic` is first-class (`{ id, subjectId, name }`).
- **Question** — 2–6 options, correct answer index, explanation, difficulty, marks, year, `status` (`draft|pending|approved|rejected`), plus a separate `isActive`/`timesUsed` pair for the approved-pool (archive) view.
- **Assessment** — title, subject, questions, duration, `status` (`draft|pending|published|archived`) — distinct from **AssessmentAssignment.status** (`active|completed|scheduled|paused`), which tracks a school's assignment of a published assessment to classes/students.
- **ExamAttempt** — per-question answers, timestamps, computed score/rank/percentile/topic-breakdown/grade (A–F), plus `tabSwitchCount`/`flaggedForReview` (anti-cheat).
- **SubscriptionPlan** — Starter/Professional/Enterprise at GHS 150/350/750/month (school audience) + several independent-student plans; nullable `programId` hook for future program-scoped billing.
- **Invoice** / **Payment** (`PAY-` prefix, `pending|completed|failed|refunded`) / **PaymentMethod** (card or Ghana MoMo, Paystack-captured only — no raw card entry).
- **AuditLog** — actor, action, category, description, polymorphic `details` JSONB — the review/moderation trail for questions, assessments, tutors, and courses.
- **Course marketplace** (added 2026-08-07): `TutorProfile` (`status: active|suspended`, no pending gate — self-service), `Course` (`status: published|flagged|removed`, no draft/pending — publish-first), `Module`/`Lesson` (video URL or article), `Enrollment`, `CoursePurchase` (own entity, not a `Payment`/`Invoice` reuse — `amount`/`platformFee`/`tutorPayout` on the row), `PlatformSettings` (singleton, currently just `platformFeePercent`, default 15).

## Key Workflows

1. **School onboarding** (`app/signup/school`) — 5-step wizard ending in `pending` verification status, not immediate activation.
2. **Student onboarding** — school-admin-provisioned (no billing) or self-service independent signup (guardian info + approval + checkout).
3. **Content pipeline** — content admin creates/uploads → `pending` → super admin reviews (`app/super-admin/review-queue`) → `approved`/`rejected` (with reason). Same pattern for assessments.
4. **Bulk upload** — CSV/XLSX, client-side parse + preview, server-side re-validated on submit, always lands as `pending` (no approval bypass).
5. **Assessment taking** (`app/student/exams/[id]/start`) — timed, eligibility re-verified server-side, grading server-side only, idempotent submit, tab-switch logged (not blocked).
6. **Billing** — school admins (`app/school-admin/subscription/**`) and super admin (`revenue`/`payments`) see real Paystack-backed `Payment`/`Subscription`/`Invoice` data. All amounts GHS.
7. **Course marketplace** (added 2026-08-07) — tutor self-signs-up → authors a course (publishes immediately) → student browses catalog → enrolls free or pays via Paystack (webhook-confirmed, idempotent) → watches lessons (`/learn`, re-verifies enrollment server-side) → super admin can flag/remove a course or suspend a tutor post-publish.

## Tech Stack

**Frontend** — Next.js (App Router) + TypeScript + Tailwind CSS v4, shadcn/ui (Radix). Deployed on Vercel (v0-linked; merges to `main` auto-deploy). **Live as of 2026-08-14 at `https://testingyourpreparedness.com`** — first real deployment, verified end-to-end (not just DNS/homepage): `/terms`, `/privacy`, `/signup`, all 4 dashboard roots return real responses (200 or an auth 307, not 404), and a real `POST /api/mobile/auth/login` against a seeded student account returned a real JWT against the live Neon DB. Getting here required one real build fix — `lib/generated/prisma` is gitignored (correctly, as generated code) but nothing was regenerating it on Vercel's `npm install`, so every build 404'd on `Module not found: Can't resolve './generated/prisma/client'` across ~8 files; fixed by adding `"postinstall": "prisma generate"` to `package.json` (standard pattern for this exact problem), verified with a full local `npm run build` before pushing. `NEXT_PUBLIC_APP_URL` in Vercel's env vars has been updated from the placeholder `http://localhost:3000` to the real domain (it's baked into Paystack's checkout `callback_url`, so this had to be right before any real payment flow could work) and redeployed.

**Backend** — Next.js fullstack (Server Actions/API routes), no separate service. Zod for validation.

**Database** — PostgreSQL via Neon (serverless), Prisma ORM. This sandbox has no IPv6 route — `net.setDefaultAutoSelectFamily(false)` + `dns.setDefaultResultOrder("ipv4first")` is required before any Prisma/Neon code runs (already baked into `lib/prisma.ts`/`prisma/seed.ts`); Neon's serverless driver also needs `neonConfig.webSocketConstructor = ws` on this project's Node 20.

**Auth** — Auth.js + own Postgres users table (no `@auth/prisma-adapter`; Credentials provider + JWT sessions).

**Payments** — Paystack (GHS pricing, mobile money support). All payment logic routed through `lib/payments/paystack.ts` — never call the SDK directly from business logic.

**File storage** — Vercel Blob, for CSV/XLSX/images. Course lesson video is an external URL, not Blob-hosted.

**Newsletter** — Decided/built 2026-08-14: Brevo (marketing/newsletter automation — a distinct concern from Resend's transactional email), confirmed with the user. `/contact` has a newsletter signup form (`subscribeNewsletter` in `app/contact/actions.ts`, rate-limited same as the contact form) that adds the email to a Brevo contact list via `lib/newsletter/brevo.ts` (same internal-interface pattern as `lib/payments/paystack.ts`/`lib/email/resend.ts` — single opt-in, `updateEnabled: true` so re-subscribing an existing contact succeeds instead of erroring). No local `NewsletterSubscriber` table — Brevo is the subscriber list's source of truth, deliberately not duplicated in our own DB since there's no other product use for it. Requires `BREVO_API_KEY` + `BREVO_NEWSLETTER_LIST_ID` (see `.env.example`); until both are set, subscribing throws a clear "not configured" error surfaced in the form, same "safe to ship without real keys" pattern as every other integration here. **Updated 2026-08-15/16**: real `BREVO_API_KEY`/`BREVO_NEWSLETTER_LIST_ID` now exist and were independently verified live (a direct API call created and then deleted a real test contact in the real "TYP-marketing" list, ID 3) — no longer unexercised. Confirmed 2026-08-16: these, along with `RESEND_API_KEY`/`EMAIL_FROM`, are set in Vercel's production environment variables too, not just local `.env`. **Extended 2026-08-15 to every account-signup path** (school, independent student, tutor, school-join), confirmed with the user first (no new DB field — checking the box just calls the existing Brevo interface directly, same "Brevo is the source of truth" call as the contact-form version): a new `lib/newsletter/brevo.ts` `subscribeToNewsletterBestEffort()` (same try/catch/log-and-swallow shape as `lib/email/resend.ts`'s `sendEmailBestEffort` — a down/unconfigured Brevo must never block account creation) is called from all four registration functions (`registerSchool`, `registerIndependentStudent`, `registerTutor`, `lib/student/join.ts`'s `createJoinedStudent` — the last one shared with the mobile `/api/mobile/auth/join` route) when a new `subscribeNewsletter` checkbox is checked. Same pass also closed a real gap found while doing this: only 2 of the 4 signup flows (school, independent student) had a "Terms of Service/Privacy Policy" agreement checkbox at all — tutor signup and the school-join flow had none. Added to both (backend-enforced on tutor/join — `registerTutor`/`createJoinedStudent` now throw if `agreeTerms` isn't true, matching the frontend's disabled-submit-button gate; school/independent-student's `agreeTerms` stays frontend-only enforced, matching those two flows' pre-existing precedent, not newly tightened here). `POST /api/mobile/auth/join` now requires `agreeTerms: true` in its request body too (a real breaking change to that route's contract, flagged to the mobile session directly since it owns the Flutter client that calls it). **Mobile side closed 2026-08-15, same day**: `JoinSchoolScreen`'s step-2 form gained the matching "I agree to the Terms of Service and Privacy Policy" (tappable `RichText` links, opened externally via `url_launcher` at `${ApiClient.baseUrl}/terms`/`/privacy` — not hardcoded to the production domain, so it follows whatever environment the app was built against) and "Send me marketing emails and feature updates (optional)" checkboxes, `ApiClient.joinSchool` now sends `agreeTerms`/`subscribeNewsletter`, and the submit button stays disabled until terms are agreed to (same disabled-until-valid pattern already used elsewhere). The terms-agreement row deliberately isn't whole-row-tappable like the newsletter one is - its label contains the two links, and a wrapping tap-to-toggle would double-fire (open the link *and* toggle the checkbox) on a link tap. Re-verified `lib/student/join.ts`'s `createJoinedStudent` live against the real DB post-change (both an `agreeTerms:false` rejection and a real `agreeTerms:true` success), not just a clean typecheck, since this was a breaking-change fix to an already-live route, not new-feature code.

**⚠️ Real breaking change 2026-08-16, mobile action needed**: school-code join no longer creates an instantly-active, instantly-signed-in account — `Student.status` starts `pending` and needs a school admin's approval (real security fix; the code was guessable and joining was previously silent to the school — see `docs/build-log.md`'s 2026-08-16 entry for the full reasoning). `POST /api/mobile/auth/join` **no longer returns a `token`** — it now returns `{ pendingApproval: true, schoolName, message }` instead. `POST /api/mobile/auth/login` now 403s a pending student with `{ error, code: "pending_approval" }` instead of succeeding. **Mobile-side Flutter work, not yet done, handed to the mobile session directly (see chat)**: `JoinSchoolScreen` needs a "request submitted, pending approval" success state instead of navigating to `HomeScreen` (mirrors `app/join/page.tsx`'s new step-3 screen); `LoginScreen` needs to show a specific message when it gets `code: "pending_approval"` instead of a generic "invalid credentials." Until this ships, a student joining via the mobile app will see the request succeed with no token and no clear next step in the UI - a real, temporary UX gap, not a crash.

**Fixed 2026-08-16, web — pressing the browser Back button right after logging in looked like it logged you out, user-reported.** Not a real logout - the session cookie was never touched - but `/login` used `router.push()` after a successful sign-in and nothing ever redirected away from `/login` when already authenticated, so pressing Back landed you back on the login form, indistinguishable from being logged out. Fixed both sides: `proxy.ts`'s matcher now also covers `/login` and redirects an already-authenticated visitor straight to their `ROLE_HOME` dashboard; `app/login/page.tsx` switched `router.push` to `router.replace` so a successful login no longer leaves `/login` in browser history at all. `npx tsc --noEmit` clean.

**Built 2026-08-16, web — new `components/confirm-dialog.tsx` (`ConfirmDialog`), the web-side counterpart to mobile's `AppDialogs.confirm`.** Wraps the existing `components/ui/alert-dialog.tsx` primitive with a shared title/description/confirmLabel/variant API, supports either a `trigger` prop or fully-controlled `open`/`onOpenChange`, and handles async `onConfirm` (disables buttons + "Please wait..." while pending). First real usage: `components/dashboard-shell.tsx`'s "Sign out" menu item previously called `signOut()` on a single click with zero confirmation, across every role's shell (one shared component) - now opens a `ConfirmDialog` first. Several pages (`student/settings`, etc.) already had their own hand-rolled `AlertDialog` confirmations before this - those work fine as-is and weren't migrated, but are natural future candidates to consolidate onto `ConfirmDialog`.

**Built 2026-08-16, web — real achievement-awarding and a real student-facing leaderboard, closing two gaps found while scoping "what would make this an instant hit."** `Achievement`/`StudentAchievement` previously existed only as data - the seed script's canonical 8-entry catalog was displayed as real on `student/progress`/`student/profile`, but nothing ever actually awarded one except the seed hardcoding 5 of them onto a single demo student ("Kwame Asante"); every other student's badge shelf was permanently empty regardless of what they did. Separately, there was no student-facing leaderboard anywhere - only `school-admin/leaderboard`/`super-admin/leaderboard` existed, and a student's own dashboard only ever showed a single "class rank" number, never a ranked list of peers (this is the gap `app/api/mobile/dashboard`'s own code comment used to explicitly call out as "nothing to mirror").
- `lib/student/leaderboard.ts` (new): `getNationalLeaderboard`/`getClassLeaderboard` (ranked lists with the caller's own rank always included even outside the returned page) plus rank-only `getStudentNationalRank`/`getStudentClassRank` helpers - shared by the new web page, the new mobile route, and the achievement checker below (one ranking implementation, not three).
- `lib/student/streak.ts` (new): `computeStreak`/`hasNInWindow`, small pure functions extracted so the achievement checker doesn't duplicate the consecutive-day-streak math `dashboard-stats.ts`/`student/progress` each already have their own inline copy of.
- `lib/student/achievements.ts` (new): `checkAndAwardAchievements(studentId)` - matches all 8 seeded criteria (`Achievement.name`, already `@unique`, matched directly rather than adding a machine-key column) against a student's real attempt history, awarding any newly-met ones for real. Called from `lib/student/exam-attempt.ts`'s `submitExamAttempt` - the one shared grading function both the web and mobile submit routes already call - so a badge can be earned from either client. Fire-and-forget from the caller's side (return value discarded); a "badge unlocked!" toast at submit time is a natural fast-follow, not built in this pass to avoid touching the web + mobile result UIs.
- `app/student/leaderboard` (new, web page) + nav item in `student-shell.tsx`: National and (if the student has a class) My Class tabs, own-rank card, ranked table with the current student's row highlighted.
- `app/api/mobile/leaderboard` (new route): same shared `lib/student/leaderboard.ts` functions, `{ national, classBoard }` shape - backend-only for now, no Flutter screen built against it yet (handed to the mobile session as a fast-follow, same pattern as the mobile account-deletion routes).
- Verified live against the real Neon DB (not just typechecked): a temp script ran `checkAndAwardAchievements` against every real seeded student with attempts - confirmed real, previously-badge-less students (Ama Serwaa, Kofi Mensah, etc.) newly earned "Perfect Score"/"Subject Master"/"National Star"/"Top 5" for real, Kwame Asante (already holding 5 pre-seeded badges) correctly only picked up the 2 he didn't already have, and a second run afterward awarded nothing new (idempotent). Leaderboard ranking/own-rank output spot-checked against the same data. Cleaned up (script deleted; the awarded rows themselves were left in place since they're real, correct results, not test pollution).
- `npx tsc --noEmit` clean (aside from one pre-existing, unrelated `tough-cookie` type-resolution error confirmed present even with these changes fully reverted - not introduced by this work, flagged to the mobile session rather than chased down here).

**Built 2026-08-16, web — a real referral system for independent students, the second of the "instant hit" feature list (after achievements/leaderboard).** Confirmed with the user first: reward is **7 free subscription days for both sides**, granted on the referee's first *paid* subscription (not at signup) - chosen over 14/30-day alternatives as the lowest-cost nudge. Scoped to independent students only (they're the ones with a personal `Subscription` a reward can extend; school-provisioned students have no billing of their own).
- `Student` gained `referralCode` (nullable `@unique`, only ever set for independent students) and a self-relation `referredByStudentId`/`referredByStudent`/`referredStudents` - additive migration, no backfill risk since both are nullable.
- `lib/referral-code.ts`: `generateReferralCode(firstName)` - short alphanumeric (name prefix + 4 random chars, ambiguous characters like `0/O/1/I` excluded since it's meant to be spoken/typed, not looked up formally), same "extract for reuse" shape as `lib/school-code.ts`.
- `app/signup/independent/actions.ts`'s `registerIndependentStudent` now generates the new student's own code and resolves an optional entered code to `referredByStudentId` - a bad/unrecognized code is silently ignored rather than blocking signup. New "Referral Code (Optional)" field added to the wizard's step 1.
- `lib/referral-reward.ts`: `grantReferralRewardIfEligible(newSubscription)` - called from the Paystack webhook (`app/api/webhooks/paystack/route.ts`) right after a brand-new student `Subscription` is created (never on an upgrade/renewal, and only once per student since `Subscription.studentId` is unique - no separate idempotency tracking needed). Extends both the referee's own new `renewalDate` and the referrer's existing one by 7 days; if the referrer has no subscription of their own yet, skips silently (a known first-pass limitation - no retroactive credit if they subscribe later).
- `app/student/settings`: a new "Refer a Friend" card (profile tab, independent students only) shows the student's own code with a copy-to-clipboard button.
- Verified live against the real Neon DB, not just typechecked: a temp script created a real referrer (with an active subscription) and a real referee referencing their code, simulated the webhook's subscription-creation call, and confirmed both `renewalDate`s extended by exactly 7 days. Cleaned up after.
- `npx tsc --noEmit --skipLibCheck` clean (the `--skipLibCheck` is only working around the pre-existing, unrelated `tough-cookie` issue flagged in the achievements/leaderboard entry above - not specific to this feature).

**Built 2026-08-17 — a real free tier with an enforced limit for independent students, closing a gap where the "Free" plan's advertised limits were never actually checked anywhere.** The `student-free` `SubscriptionPlan` row has always listed "5 practice tests/month," "Basic score reports," and "Limited question bank" as its terms, but every independent student — paid or not — had unlimited access to everything until this pass.
- New `lib/student/entitlement.ts`: `getStudentTier()` (a student's tier is derived, not a stored flag — no `Subscription` row, an inactive one, or a lapsed `renewalDate` all resolve to `"free"`; school-provisioned students are always `"paid"` since their school carries the billing) and `getFreeTierAttemptsUsedThisMonth()` (a rolling 30-day window, not calendar-month, so a student doesn't get a fresh batch of 5 on the 1st regardless of when they last used one).
- `lib/student/exam-attempt.ts`'s `resolveExamEligibility` now checks tier before letting an independent student start a new attempt, returning `{ eligible: false, reason: "free_tier_limit" }` at the cap — an already-started, not-yet-submitted attempt never consumes a slot, only a fresh submission counts. `lib/student/result-detail.ts` returns a `detailedReportsLocked` flag for free-tier results (rank/percentile/class comparison/topic breakdown withheld, matching the "basic score reports" promise) rather than omitting the fields outright.
- `app/student/settings`: a new "Plan" tab (independent students only) shows the current tier and a real "Upgrade" button wired to the existing `initializeStudentCheckout` — previously the only way to ever pick a paid plan was during initial signup.
- Also fixed in the same pass: two overstated homepage claims ("thousands of students already trust TYP") corrected to match the platform's actual current scale.
- Mobile: `app/api/mobile/exams/[id]/start`/`results/[attemptId]` surface the same `free_tier_limit` reason and `detailedReportsLocked` flag; `ResultDetail`/`ResultsScreen` render the locked-report state.
- Built by a peer session, not independently live-DB-verified by this session — `npx tsc --noEmit --skipLibCheck` passes across the full repo including these files, but the verification methodology/live-DB check described elsewhere in this file for other features hasn't been independently repeated here.

**Built 2026-08-17 — a mobile "Upgrade Plan" screen, closing the fast-follow the free-tier entry above deferred, plus two user-requested home-screen nav fixes.** `lib/student/subscription.ts` (new) extracts `initializeSubscriptionCheckoutForStudent` out of `app/signup/independent/actions.ts`'s `initializeStudentCheckout` (same "one function, two callers" split as `lib/student/courses.ts`, no auth check inside — each caller does its own), backing three new mobile routes (`GET/POST /api/mobile/subscription`, `.../checkout`, `.../verify`) that reuse the exact Paystack flow the web upgrade button already uses. Mobile gained `models/subscription.dart`, matching `ApiClient` methods, a `subscription_checkout_webview_screen.dart` (copy of the course-purchase webview's interception pattern), and `screens/upgrade_plan_screen.dart` (current-plan/usage card + plan-cards-with-Upgrade-buttons for free-tier students). `home_screen.dart`: bottom nav reordered to Dashboard/Exams/Courses/Profile (Dashboard now first on login, previously Exams), Profile promoted from an AppBar icon to a real nav tab, and the Exams tab's AppBar "sign out" icon replaced with an "Upgrade Plan" icon (Log Out unchanged, still in Profile → Settings — nothing removed, just de-duplicated). `npx tsc --noEmit --skipLibCheck` clean; **not `flutter analyze`/`flutter test`-verified** — Flutter CLI unavailable in this sandbox, same standing gap as other mobile work — checked by hand for import/API-shape correctness instead.

**Fixed 2026-08-17, same day**: the first real mobile CI run against this work failed `flutter analyze` on 2 pre-existing (unrelated to the above) `curly_braces_in_flow_control_structures` violations in `mobile/lib/screens/login_screen.dart` — this project's mobile CI treats any analyze finding as a hard failure, even "info" severity. Fixed both; a repo-wide sweep for the same pattern found no other real violations (19 other candidates were all Dart's collection-`if` widget-list syntax, which the lint doesn't apply to).

**Built 2026-08-17 — a free-tier upgrade push nudge, fired the moment a student actually hits the practice-test cap** (via `startOrResumeExam` in `lib/student/exam-attempt.ts`, not on every page view). Reuses the existing `sendPushToStudentBestEffort`/exam-push scope (confirmed 2026-08-16) — no new vendor. Push-only, not email; not deduplicated across repeat blocked attempts. Same day: `mobile/lib/services/push_notification_service.dart` gained real tap deep-linking (previously just logged) — `exam_assigned`/`results_ready`/`free_tier_limit_reached` taps now open the right screen (including from a terminated cold-start via `getInitialMessage()`, not just backgrounded), so the nudge above is now one tap from converting. Same day, found by an explicit web/mobile parity audit (not assumed): `ExamTakingScreen`'s free-tier-block error view had no path to the Upgrade Plan screen (web's equivalent redirect has a link, mobile's `ApiClient` was dropping the server's `code` field on every non-login call) — fixed, `ErrorView` gained an optional `secondaryAction` slot for this. See `docs/build-log.md` for full detail.

**Built 2026-08-17, web — platform-hosted lesson video (Mux) and course-scoped virtual tutoring sessions (Daily.co + external links), the third/fourth items to come out of "what would make this an instant hit."** Both vendors confirmed with the user first (Mux over reusing Vercel Blob - not built for video streaming at scale; Daily.co over Agora/Stream Video - prebuilt embeddable call UI, fastest path to a working native call). Virtual sessions are course-scoped group sessions, not 1:1 tutor/student bookings, confirmed with the user - every enrolled student can join, matching how course access already works.
- `prisma/schema.prisma`: `Lesson` gained `videoSource`/`muxUploadId`/`muxAssetId`/`muxPlaybackId`/`muxStatus` - additive, tutor's per-lesson choice, external URLs still fully supported (not a replacement). New `VirtualSession` model (course-scoped, `mode: daily | external_link`, `dailyRoomName`/`dailyRoomUrl`/`externalMeetingUrl`, `status: scheduled|live|ended|cancelled`).
- `lib/video/mux.ts` (new): `createDirectUpload()` (browser uploads straight to Mux, never through our server), `deleteAsset()`, `verifyWebhookSignature()`. `app/api/webhooks/mux/route.ts` (new): Mux's asset processing is async - this is the source of truth for "is the video actually playable," matches the finished asset back to a `Lesson` row via `muxUploadId` (known immediately at upload time, before the asset exists).
- `lib/video/daily.ts` (new): `createRoom()`/`deleteRoom()`. Rooms created/expired around each session's scheduled time.
- Tutor authoring: new `components/video-lesson-input.tsx` (shared by `create-course-form.tsx` and `course-detail-view.tsx`'s lesson forms) toggles between "Paste URL" (original) and "Upload Video" (direct-to-Mux). New `app/tutor/courses/[id]/virtual-sessions-section.tsx`: schedule/cancel sessions, mode picker (native call vs external link), copy-join-link.
- Student-facing: `lesson-viewer.tsx` plays Mux video via `@mux/mux-player-react` when ready (shows a "still processing" state otherwise), falls back to the original external-link button for non-Mux lessons. `course-detail-purchase-view.tsx` shows upcoming sessions with a Join button gated on `isEnrolled` (matches the existing "gate in the view, not just the query" pattern used for reviews/myReview). Both `lib/student/courses.ts` functions (`getCourseDetail`/`getLearnContent` - the shared functions the mobile API routes already reuse) now include the new fields, so mobile gets this data for free without any backend route changes.
- Mobile Flutter client work (Mux HLS playback, joining a Daily room or external link) explicitly not built in this pass - handed to the mobile session as a fast-follow (same "ship the backend/web, hand off the client" precedent as offline downloads and the student leaderboard).
- Verified live against the real Neon DB (not just typechecked): a temp script created a `Lesson` with Mux fields, simulated the webhook's ready-update, created both a `daily`-mode and `external_link`-mode `VirtualSession`, confirmed the `Course.virtualSessions` relation query works, cancelled one and confirmed the status change, cleaned up after. Mux/Daily.co API calls themselves are unexercised (no real keys in this environment) - same "safe to ship unconfigured" pattern as every other integration here.
- `npx tsc --noEmit --skipLibCheck` clean.

**Background jobs** — Not yet decided; bulk upload stays synchronous client-side-parse (≤5MB, per current UI copy).

**Mobile** — Decided/started 2026-08-10: a Flutter app (iOS + Android), living in `/mobile` inside this same repo (not a separate repo), talking to new REST routes under `app/api/mobile/**` in this same Next.js app (no separate backend service). **v1 scope is student-only**: login → see assigned exams → take a timed exam → results/leaderboard — not the course marketplace, not other roles, until this loop proves out. `app/api/mobile/auth/login` (student-only, 403s any other role), `app/api/mobile/exams` (available/scheduled/completed) and `app/api/mobile/me` exist, plus the full exam-taking loop: `app/api/mobile/exams/[id]/start` (find-or-resume, same no-clock-reset-on-relaunch guarantee as web), `app/api/mobile/attempts/[attemptId]/submit` (idempotent, server-side graded), `app/api/mobile/attempts/[attemptId]/tab-switch` (anti-cheat), `app/api/mobile/results/[attemptId]` (full score/rank/topic-breakdown detail), and `app/api/mobile/dashboard` (stats/streak/class-rank/trend/subject-strengths/recent-results, mirroring the web dashboard — there is no student-facing leaderboard anywhere in the web app to mirror instead, only admin-facing ones, so none was invented for mobile) — all backed by `lib/student/exams.ts`/`exam-attempt.ts`/`result-detail.ts`/`dashboard-stats.ts`, the same shared functions the web app's own student pages call, not a second copy of the business logic. The Flutter client (`lib/main.dart`, `screens/`, `services/api_client.dart` + `token_storage.dart` using `flutter_secure_storage` for the bearer token, `models/`, a `theme/app_theme.dart` converted from the web app's real `app/globals.css` OKLCH tokens rather than a separately-invented palette) now covers the full v1 loop: login, the 3-tab exams list, a real exam-taking screen (`ExamTakingScreen` — countdown timer seeded from `remainingSeconds`, `WidgetsBindingObserver`-driven tab-switch logging on `paused`/`inactive`, auto-submit at zero, resume-safe), and a results screen (`ResultsScreen`, reused both after a real submit and for tapping a past completed exam). **Decided/built 2026-08-15 — mobile self-registration, confirmed with the user first (a real scope expansion beyond v1's original login-only student flow)**: the school-code join path only (`app/join`'s web equivalent) — the independent-student path (subscription plan + real Paystack checkout) is a deliberate, explicitly-deferred follow-up, not built here. `lookupSchoolByCode`/`createJoinedStudent` extracted out of `app/join/actions.ts` into `lib/student/join.ts` (same "one function, two callers" pattern as `lib/student/courses.ts`/`dashboard-stats.ts` — `app/join/actions.ts` refactored to call it too, still doing its own `enforceRateLimit`, not a second copy of the validation/creation logic), backing two new routes: `POST /api/mobile/auth/join/verify` (school-code lookup) and `POST /api/mobile/auth/join` (creates the account, returns the same `{ token, user }` shape as `/api/mobile/auth/login` so the client signs straight in) — both IP-rate-limited via `checkRateLimit`/`getClientIpFromHeaders` (the Route Handler equivalent of `enforceRateLimit`, since `next/headers()` needs a Server Action's request context). The Flutter client gained `JoinSchoolScreen` (2-step: verify code → fill in details, mirroring `app/join/page.tsx`'s UX) plus `VerifiedSchool` in `models/user.dart` and `ApiClient.verifySchoolCode`/`joinSchool`, linked from a new button on `LoginScreen`. `flutter analyze`/`flutter test` clean; `lib/student/join.ts`'s two functions were also smoke-tested directly against the live Neon DB (real school code lookup + real student creation + cleanup) rather than only trusting a clean typecheck.

**Fixed 2026-08-15 — the mobile app icon/splash were stale.** User-reported: the launcher icon and splash mark were a leftover glyph from an earlier/different branding pass (not the real product mark), invisible to this file's earlier "checked for real pixel content" verification since that check only confirmed the assets weren't blank, not that they matched the current brand. Confirmed with the user: app name stays **TYP** (already correct everywhere — `AndroidManifest.xml`'s `android:label`, iOS `Info.plist`'s `CFBundleDisplayName`, `main.dart`'s `MaterialApp.title` all already said "TYP", matching the web tab title "TYP - Testing Your Preparedness" — this was an icon-only fix, no name change needed); logo source is the real current web favicon/navbar mark (`public/icon.png`, the shield+checkmark — `public/icon.svg` referenced in this file's earlier 2026-08-10 entry no longer exists, the web app's icon shipped as a rasterized PNG since). Regenerated all three `mobile/assets/branding/` source files from it: `app_icon.png` (full-color mark on white, for iOS/legacy Android), `app_icon_foreground.png` + `splash_logo.png` (white silhouette derived from the source PNG's own alpha channel, on transparent, for the adaptive-icon-foreground/splash layers over the existing brand-blue background) — same "white mark on `#0072D5`" formula as before, just with the real shield+check shape instead of the old placeholder glyph. Re-ran `flutter_launcher_icons`/`flutter_native_splash` to regenerate all 60 derived platform assets. Verified by compositing the transparent-background outputs onto brand blue before visual inspection (a bare Read render of a white-on-transparent PNG looks blank against this tool's white viewer background - not a defect) and confirming real alpha-channel bounding boxes, not just "generator exited 0."

**Large mobile session, 2026-08-16 (still uncommitted as of this writing - re-documented after an earlier version of these entries was lost to a docs-file overwrite race with a concurrent session; see `docs/build-log.md`'s matching entry for the full per-item breakdown).** In order: (1) fixed the real breaking change from `POST /api/mobile/auth/join` gaining school-admin approval-gating - `JoinSchoolScreen` now shows a "request submitted, pending approval" screen instead of navigating into `HomeScreen`, `LoginScreen` surfaces the `pending_approval` 403 message. (2) Splash screen now stays up a real minimum 3s (`FlutterNativeSplash.preserve()`/`.remove()`) instead of vanishing the instant the first frame rendered. (3) A full UI modernization pass across every screen in the app - `google_fonts` (Inter/Space Grotesk, matching web's own pairing), richer Material theming (buttons/inputs/nav/cards/chips/dialogs) in `theme/app_theme.dart`, restyled cards/headers/empty-states everywhere. (4) Mobile self-service account deletion, initially on `profile_screen.dart` then moved to a new dedicated `settings_screen.dart` (Log Out + Danger Zone) per the user's explicit call that account actions belong under Settings, not Profile. (5) A shared `widgets/app_dialogs.dart` (`AppDialogs.confirm`/`.info`) replacing scattered one-off `AlertDialog`s - closed a real gap where Log Out had zero confirmation from either of its two entry points. (6) Fixed a real, user-reported "back button logs me out" bug with two separate causes: `HomeScreen` had no `PopScope` so a bare back press killed the app outright (now a proper "press back again to exit" pattern), and `AuthGate._check()` in `main.dart` was treating *any* profile-check failure - not just a real 401 - as grounds to wipe the stored session, so a relaunch into a connectivity blip looked exactly like a logout (now falls back to a small locally-cached `{id, name, email}` via `TokenStorage.saveCachedUser`/`readCachedUser`, only a real 401 actually logs out). (7) Pull-to-refresh consistency pass across screens that were missing it. (8) User-requested: real app version display on Settings (`package_info_plus`), an app-wide "you're offline" banner sourced from real OS connectivity state (`connectivity_plus`, wrapped via `MaterialApp.builder`), and a real accessibility pass (icon-button tooltips, `MergeSemantics` on a checkbox row, two touch targets brought up to Material's 48dp minimum, and computed - not assumed - WCAG contrast ratios for every theme color pair, all passing AA). (9) Fixed a real, user-reported bottom-content-clipping bug on `results_screen.dart` (the assessment/exam review screen) and 5 other pushed screens - none had protection from an edge-to-edge device's gesture-nav bar; fixed via a new shared `screenScrollPadding()` helper (adds the real `MediaQuery` bottom inset) or `SafeArea` where a flat list didn't apply. (10) Real light/dark theme control, user-requested after noticing the app rendered fully dark in their last APK with no way to change it - root cause was `MaterialApp` never setting an explicit `themeMode` at all, silently inheriting the device's own system setting. New `services/theme_controller.dart` (`ThemeController`, a `ChangeNotifier` singleton) defaults to `ThemeMode.light` (not `system`), persists via the same `flutter_secure_storage` instance `TokenStorage` uses, and `settings_screen.dart` got a real "Appearance" card with a `SegmentedButton<ThemeMode>` (Light/Dark/System). (11) Fixed the production-build footgun this same theme conversation surfaced: `ApiClient.baseUrl`'s default was `http://localhost:3000` - a plain `flutter build apk --release` with no `--dart-define` flag silently baked in an unreachable URL on a real device, with no obvious error. Default flipped to the real production URL (`https://testingyourpreparedness.com`); local development now opts in via `--dart-define=API_BASE_URL=...` instead of production builds needing to remember it. (12) Final pre-build polish, user-requested: haptic feedback on tab switches/dialog confirmations/star-rating taps/exam answers/exam submit success; a ~180ms crossfade on `HomeScreen`'s bottom-nav tab switches (wraps the same persistent `IndexedStack` rather than swapping widget instances, so tabs don't lose state/re-fetch on every switch); and hand-rolled shimmer skeleton loaders (`widgets/skeleton.dart`, no new dependency) replacing the bare spinner on the 3 highest-traffic first-load screens (Exams, Dashboard, Course catalog), each shaped to mirror its real screen's layout.
`flutter analyze`/`flutter test` clean throughout all of the above. **⚠️ Same standing caveat as this whole pass: no emulator/device available in this sandbox** - everything above is code-review-verified only. Item (10), the theme fix, is the single highest-value thing to confirm on a real device first, since it's the one directly, mechanically responsible for what the user actually observed on their own phone.

**Decided/built 2026-08-15 — splash screen follow-up, two user-reported issues after the icon fix above.** (1) The bare centered icon read as an unfinished design, not a real splash - added a `brand_wordmark.png` (a "TYP" wordmark rendered from DejaVuSans-Bold, no closer brand-matched font file available in this sandbox and no external font fetch attempted) pinned near the bottom via `flutter_native_splash`'s `branding`/`branding_mode: bottom` config, a genuinely separate unmasked layer from the centered `image` (which Android 12+ clips to its own icon mask/animation, so a wordmark couldn't have lived there anyway). (2) The native Android 12+ icon zoom/fade played very fast - `flutter_native_splash` has no config key for `windowSplashScreenAnimationDuration` at all (confirmed by reading the installed package source, not assumed), so `values-v31/styles.xml` and `values-night-v31/styles.xml` were hand-edited to add it at `1000` (the OS-enforced ceiling for this attribute - a higher value is silently capped, so this is as slow as a single native run can go). **This hand-edit does not survive a future `flutter_native_splash:create` regeneration** - both files now carry an inline comment flagging that it needs re-adding after any such regen; a real gap, not treated as fully closed. Every regenerated asset was again verified by compositing onto the real brand-blue background before trusting it, same precedent as the icon fix. **Fixed 2026-08-16**: even with the 1000ms icon-animation cap, the splash still vanished "in a flash" - because Android's native SplashScreen is dismissed on Flutter's *first rendered frame*, and `AuthGate`'s `build()` returns a near-instant loading `Scaffold` regardless of how slow the *icon's own* zoom/fade plays. Fixed at the Dart-side runtime API `flutter_native_splash` also ships (`FlutterNativeSplash.preserve()`/`.remove()`, not previously used - moved the package from `dev_dependencies` to `dependencies` in `pubspec.yaml` since a dev-only package can't be imported into shipped app code): `main()` now calls `preserve()` before `runApp()`, and `AuthGate._check()` calls `remove()` only once both its real auth check *and* a minimum 3-second elapsed delay have completed (`Future.delayed` for the remainder, not a flat 3s sleep - so a slow auth check never gets held even longer). `flutter analyze`/`flutter test` clean; no real-device visual confirmation possible in this sandbox (no Android SDK/Xcode), same standing caveat as the rest of this project's un-exercised mobile-build items - needs the user's own device to confirm the on-screen timing feels right.

**Decided/built 2026-08-15**: `GET /api/mobile/dashboard` now has a real client screen (`screens/dashboard_screen.dart`, `models/dashboard.dart`) — stat tiles (exams completed, average score, streak, class rank), a 6-month performance trend (plain `Container`-based bars, no chart package added — this app's existing "no new dependency for a small visual" precedent), subject-strength progress bars, and recent results (tappable into the existing `ResultsScreen`). Wired into `HomeScreen` as a third bottom-nav tab (Exams / Dashboard / Courses). `flutter analyze`/`flutter test` both clean. **Real Android/iOS native project scaffolding now exists too** (a real `flutter create`, not the empty placeholder directories from the initial scaffold) — `flutter analyze` and a real `flutter test` are both clean/passing against it, though no actual native build has been exercised (no Java/Android SDK or Xcode/macOS in this sandbox, same standing caveat as this project's other un-exercised external integrations). **Real launcher icons and a splash screen are now in place too**, generated from the web app's actual `public/icon.svg` mark (not a generic Flutter icon or an invented logo) via `flutter_launcher_icons`/`flutter_native_splash` (standard build-time codegen packages, flagged/uncontroversial, not a new runtime vendor). The source SVG's colors only existed inside a `prefers-color-scheme` media query that icon tooling can't evaluate (confirmed: rasterizing it as-is produces a fully blank/transparent image) — confirmed with the user which fixed color scheme to bake in instead: brand primary blue (`#0072D5`, `app_theme.dart`'s real light-theme token, same OKLCH source of truth as the rest of the theme) background with the white mark, not the web favicon's literal black/white variant. Source assets live in `mobile/assets/branding/`. Every generated icon/splash asset was checked for real pixel content (dimensions + pixel-variance, not just "generator exited 0") before being trusted. **App-wide 401 handling and a consistent loading/error/empty-state pattern are now in place too**: `ApiClient`'s authenticated calls all route through one shared `_authorizedRequest` helper (`api_client.dart`) that clears the stored token and redirects to `LoginScreen` (via a `rootNavigatorKey`/`navigation_service.dart`, since `ApiClient` has no `BuildContext` of its own) on any real `401` — from any screen, not just the cold-start check — with a "session expired" banner distinguishing it from a normal bad-password error; a real 429/403/404 is left alone as a specific answer, not treated as an auth problem. A new shared `widgets/async_state_views.dart` (`LoadingView`/`ErrorView`/`EmptyView`, plus an `errorMessageFor` helper) replaced 4 screens' worth of near-identical hand-rolled loading/error/empty UI — deliberately just 3 small stateless widgets, not a state-management framework. **Mobile scope expanded 2026-08-10 to the course marketplace** (browse → detail → purchase/enroll → watch), mirroring `app/student/courses/**` the same way the exam loop mirrors `app/student/exams/**` — `lib/student/courses.ts` extracted the catalog/detail/my-courses/learn/enroll/purchase-init/purchase-verify/review logic out of `app/student/courses/{page,[id]/page,my/page,[id]/learn/page,checkout/callback/page,actions}.tsx` (those web files refactored to call it too, not just a mobile-side copy), backing new `app/api/mobile/courses/**` routes. **Payments**: course purchase opens Paystack's real hosted checkout (`initializeCoursePurchaseForStudent`, reusing `lib/payments/paystack.ts` — no separate mobile payment path, no native card form) inside an in-app `webview_flutter` view that intercepts navigation back to the same real web callback URL (`/student/courses/checkout/callback`) before it loads, extracts the reference, and calls a new `GET /api/mobile/courses/purchase/verify` for a native result dialog — the Paystack webhook remains the sole source of truth for creating the `Enrollment`, same as web. **Video**: lesson videos are opened externally (`url_launcher`, system browser/app), matching the web app's own `lesson-viewer.tsx` exactly (an `<a target="_blank">`, not an embedded player) — deliberately not a native video-embed package, since lesson videos can be any external host a tutor pasted in. Review *submission* (rating/comment) is a fast-follow, not built in this pass — the catalog/detail screens do show existing reviews/average rating (free from the same `getCourseDetail` payload). **Built 2026-08-15**: that fast-follow closed — a new `POST /api/mobile/courses/[id]/review` route calls the same `submitCourseReviewForStudent` (upsert, enrollment-gated) the web `submitCourseReview` action already used, and `course_detail_screen.dart` gained the matching form (`StarRatingInput`, a new interactive companion to the existing read-only `StarRatingDisplay`), shown only when `course.isEnrolled` — same gate as web. Reviews stay student-facing only: a tutor's own course detail page (`app/tutor/courses/[id]`) shows just an aggregate average-rating number, never the review list/comments, and there's no super-admin review view at all — nothing in this pass changed that visibility model, it was already the case on web. Couldn't be smoke-tested against the live DB the way the join flow was — **zero real `Enrollment` rows exist yet** (see `PROGRESS.md`'s course-marketplace track status), so there's no real student+course pair to submit a review against; relied instead on a full-repo `tsc --noEmit` plus the fact that `submitCourseReviewForStudent` itself is the same function the working web feature already calls. `HomeScreen` restructured around a bottom `NavigationBar` (Exams / Courses) with each tab keeping its own full `Scaffold` via `IndexedStack`, rather than merging two differently-shaped AppBars into one. Push notifications were explicitly *not* started in this pass — needed a real FCM/APNs vendor decision from the user first, per this file's "flag new dependencies" rule; `webview_flutter`/`url_launcher` themselves are official Flutter-team client packages (no new account/vendor/API key), flagged here for the record rather than escalated, same precedent as `flutter_launcher_icons`/`flutter_native_splash`. **Built 2026-08-16 — that decision made and the feature shipped**: Firebase Cloud Messaging (confirmed with the user), scope confirmed as exam-related only for v1 (new exam assigned, results ready). Backend: a new `DeviceToken` model (studentId, unique token, platform - one row per signed-in device, upserted on the token itself so a rotated/reissued token doesn't create a duplicate row), `lib/push/fcm.ts` (`sendPushToStudentBestEffort`, same internal-interface/best-effort-fails-open pattern as `lib/email/resend.ts` - throws a clear "not configured" error without `FIREBASE_SERVICE_ACCOUNT_JSON` set, caught by every caller), `POST /api/mobile/push/register`/`.../unregister`. Wired at the two confirmed trigger points: `app/school-admin/assessments/assign/actions.ts` (alongside the existing assignment email) and `lib/student/exam-attempt.ts`'s `submitExamAttempt` (shared by both web and mobile submit paths - fires for any student with a registered token regardless of which client submitted). Mobile: `firebase_core`/`firebase_messaging` added, new `services/push_notification_service.dart` (permission request, token registration/refresh on login and cold start, foreground messages shown via an in-app `SnackBar` since the OS doesn't auto-show a system notification while the app's open, background/terminated taps logged but not yet deep-linked - a real fast-follow, not built in this pass), token unregistered on both logout call sites before the session actually clears. **A placeholder `mobile/lib/firebase_options.dart` exists so the app compiles and `flutter analyze`/`flutter test` pass without a real Firebase project** - `Firebase.initializeApp()` is expected to fail against its fake values, caught in `PushNotificationService`, so push is silently inert until real project credentials exist (that file's own doc comment has the exact `flutterfire configure` steps needed to replace it for real). `android.permission.POST_NOTIFICATIONS` added to `AndroidManifest.xml` (required on Android 13+ for the permission prompt to appear at all). `DeviceToken`'s migration applied and live-DB-smoke-tested (create/query/delete round trip against a real student row, not just typechecked); backend `tsc --noEmit --skipLibCheck` and mobile `flutter analyze`/`flutter test` all clean. **Real send-a-real-notification is still unexercised** - same "safe to ship, unexercised until real keys exist" pattern as every other integration in this project; needs a real Firebase project + service account (backend) and a real `flutterfire configure` run (mobile) before this can be tested end-to-end. (Note: `npx tsc --noEmit` without `--skipLibCheck` is currently failing repo-wide on an unrelated pre-existing `tough-cookie` type-resolution error, confirmed via a peer session's `git stash` test to predate this feature - `--skipLibCheck` is the working verification path until that's separately chased down.) **Also found and fixed while building this**: the Android manifest had no `<uses-permission android:name="android.permission.INTERNET" />` at all — a real, pre-existing gap since the very first scaffold that would have silently broken every API call (not just the new webview) on an actual device, invisible to `flutter analyze`/`flutter test` since neither exercises Android's real permission model; also added `usesCleartextTraffic`/iOS `NSAllowsLocalNetworking` (scoped to local-network requests only) so local-dev's `http://` `baseUrl` and LAN-IP physical-device testing actually work. See `docs/build-log.md`'s 2026-08-10 entries.

**Fixed 2026-08-14**: the first real-device install attempt (of a CI-built APK) failed with "App not installed." Root cause: `.github/workflows/mobile-ci.yml`'s `build-android-apk` job runs on a fresh GitHub Actions runner every time, which has no `~/.android/debug.keystore` yet — so `flutter build apk --release` (signed with the debug config per `build.gradle.kts`'s own pre-existing TODO) generated a brand-new random signing certificate on every single CI run. A device with an earlier CI build already installed can't accept a newer one signed with a different certificate; Android refuses it outright rather than upgrading. Fixed with a real persisted release key: `mobile/android/app/build.gradle.kts` now reads an optional `key.properties` (gitignored, Flutter's own template convention) and signs with it when present, falling back to the debug key otherwise (so plain local `flutter run --release` still needs zero setup). The workflow gained a manually-triggered `generate-keystore` job (`workflow_dispatch` only) that creates this keystore once via `keytool` and hands it back as a short-lived artifact for the user to save as two repo secrets (`ANDROID_KEYSTORE_BASE64`, `ANDROID_KEYSTORE_PASSWORD`); `build-android-apk` provisions `key.properties` from those secrets when set. **Still self-signed, not a Play Store key** — same standing TODO as before, just now stable across builds instead of different every time. Same commit also fixed a second, unrelated real bug: the APK build never passed `API_BASE_URL`, so every build baked in the `http://localhost:3000` dev default — harmless before any real deployment existed, but silently broken now that `testingyourpreparedness.com` (see Tech Stack's Frontend entry) is live, since a phone can't reach the build machine's own localhost. `flutter build apk --release` now passes `--dart-define=API_BASE_URL=https://testingyourpreparedness.com`. **Confirmed 2026-08-15 (user-verified): a real CI run and a real device install both succeeded** — the persisted-keystore signing and baked-in production API URL fixes are now verified end-to-end, not just code-reviewed. Closes this file's last standing "unexercised" caveat for the mobile CI pipeline. Still self-signed (not a Play Store key) and there's no app-store presence (Play Console / App Store Connect listing) yet — that's real remaining work, not a bug.

## Architectural Principles

1. **The frontend is the source of truth for product behavior.** Don't redesign flows/statuses/permissions while building the backend — match what's built in `app/**`. Raise anything that seems wrong as a question, don't silently change it.
2. **No vendor lock-in beyond what's been deliberately decided.** Neon/Prisma/Auth.js/Paystack/Vercel Blob are the agreed stack — flag any new SaaS dependency first.
3. **Payment logic goes through an internal interface**, never called directly from route handlers/business logic.
4. **Role/tenancy scoping is a hard requirement.** Every school-admin/student/tutor query is scoped server-side, not just hidden in the UI.
5. **Approval/moderation workflows are core, not incidental.** Exam-prep content is gated `pending → approved/rejected` server-side; the marketplace is publish-first but still has a real server-side moderation lever (`flagged`/`removed`/tutor `suspended`).

## Repo / Workflow Conventions

- Source of truth: GitHub. This repo is v0-linked — v0 chat UI pushes commits directly to `main` and auto-deploys. Pull before starting work.
- **Commit message style**: single-line Conventional Commits — `type: short imperative summary`, no body/footer, no `Co-Authored-By` trailer. Group unrelated changes into separate commits, not one giant dump — but see the batching rule immediately below before committing at all.
- **Batch enough real work before committing - don't commit after every single small thing (added 2026-08-16, direct user feedback).** Committing immediately after each individual fix/tweak within one continuous task produces too many small, bulky commits and noisy history - the opposite of the goal. Default to committing once per **logically complete unit of work** (a whole feature, a whole audit-and-fix pass, everything from one coherent user request), not once per file touched or once per sub-step along the way. Rule of thumb: if you're still actively iterating on the same task you started five minutes ago, keep going - don't stop to commit yet. Only commit sooner than that when a natural, genuinely separate concern shows up mid-task (e.g. an unrelated bug noticed in passing) and even then, prefer finishing the current unit first. This does not relax the "typecheck clean before every commit" discipline - it only changes *when* you commit, not the quality bar for each one.
- **Docs updates ride with the change they document (revised 2026-08-15)**: `CLAUDE.md`/`docs/build-log.md`/`PROGRESS.md` edits for a change go in the *same* commit as that change, not a trailing separate `docs:` commit. Original reasoning for splitting them was one-commit-per-concern; reversed because a docs-only commit is never independently useful (no one reviews "docs: record X" without X's own diff next to it) and, with multiple parallel Claude Code sessions and no PR gate, the doubled commit count was making `git log`/`git blame` noisier for no real benefit. Genuinely unrelated changes (e.g. an unrelated `.gitignore` chore) still get their own commit — this only merges a change with the docs that describe that same change.
- **Branch strategy (decided 2026-08-08)**: everyone — v0 and Claude Code sessions alike — commits and pushes directly to `main`, no feature branches or PR review. Deliberately kept as-is rather than moving to a branch+PR flow, since v0's own auto-push-to-main behavior can't realistically be changed and a PR flow for only half the contributors wouldn't actually protect `main`. Relies on discipline instead: typecheck clean before every commit, small logical commits (not one giant dump), and this file's existing "confirm before destructive git operations" rule — not branch protection.
- **Claude Code sessions: never run `git commit` on your own initiative — always stop and ask the user first, even when a logical unit of work is clearly finished (added 2026-08-17, direct user feedback).** Reason: with several Claude Code sessions active in parallel, autonomous commits (and the doc/schema writes that ride with them) repeatedly collided — `prisma/schema.prisma`, `CLAUDE.md`, `PROGRESS.md`, and `docs/build-log.md` were each overwritten mid-edit at least twice on 2026-08-17 alone by a concurrent session's uncoordinated read-modify-write, silently losing real content until someone noticed and reconstructed it from memory (see this file's "Provenance note" entries in `docs/build-log.md` for the recoveries). Prompting the user before every commit gives a natural checkpoint to catch this instead of letting several sessions independently decide it's time to commit. This doesn't apply to v0's own pipeline (out of Claude Code's control) or block finishing/verifying work — only the actual `git commit` invocation needs the pause.

## Open Decisions (update as resolved)

- [ ] **Payment/purchase receipt emails** (added 2026-08-15) — course purchases and subscription payments send zero receipt/confirmation email today. Real gap, deliberately deferred rather than bundled into the 2026-08-15 email-notification batch, since it touches the Paystack webhook's idempotency logic (`app/api/webhooks/paystack/route.ts`) and needs its own careful pass, not a quick add. User explicitly asked this stay remembered for a future session.
- [ ] **Question/assessment approval confirmation emails** (added 2026-08-15) — the success-case mirror of `contentRejectedEmail` (built same day). Lower priority than rejection since there's no actionable feedback to deliver, just a nice-to-have confirmation - flagged, not built.
- [x] Offline course-lesson download, mobile-only — resolved 2026-08-17, built by the mobile session (`v0-typ-b0`): article-type lessons only, sandboxed to the app (OS-private storage, no export/share), explicit per-lesson download action, a download-manager screen. Closes the last of the original 4-item "instant hit" list (streaks/badges/leaderboard, referral system, and Mux/virtual-sessions below all shipped the same window).
- [x] **Mux video playback + Daily.co/external-link virtual-session joining, mobile** — resolved 2026-08-17, built by the mobile session: lesson videos with `videoSource === "mux"` and `muxStatus === "ready"` now stream in-app via `video_player`+`chewie` against Mux's HLS URL, and a student can join a scheduled `VirtualSession`'s `dailyRoomUrl`/`externalMeetingUrl` externally via `url_launcher` (matching web's own "open externally" Join button exactly, not a native call embed). Zero backend changes needed - the shared `lib/student/courses.ts` functions already returned the fields. See `docs/build-log.md`'s 2026-08-17 mobile entry.

- [x] Background job handling for bulk upload / async work — resolved 2026-08-08: bulk upload (`content-admin/questions/upload`, `school-admin/students/add`) stays synchronous, not queued (real usage is well within a safe processing window), now with a hard row cap (300/200 respectively) so an oversized file fails fast with a clear error instead of timing out silently. Scheduled/recurring work uses Vercel Cron (native platform feature, no new vendor). See `docs/build-log.md`.
- [x] Branching/PR workflow — resolved 2026-08-08: stays direct-to-`main` for everyone (v0 and Claude Code alike), see Repo/Workflow Conventions above.
- [x] Platform-wide config store beyond the marketplace fee — resolved/built 2026-08-15: `PlatformSettings` grew `platformName`/`supportEmail` (confirmed with the user, who scoped it to just these two fields, not timezone/feature-toggles), editable via a new "Platform" tab on `super-admin/settings` (same upsert-singleton/audit-log pattern as the existing marketplace-fee field). Deliberately not yet wired into the public contact page or email templates, which still use their own hardcoded values — this pass is the config store itself, not the consuming call sites. See `docs/build-log.md`'s 2026-08-15 entry.
- [ ] Third-party API/webhook integration surface (no external integrations requested yet)
- [x] PDF/Excel export for `super-admin/reports` — real, built 2026-08-08 (all 3 datasets, `xlsx` reused + `jspdf`/`jspdf-autotable` added), including scheduled/recurring generation (a weekly Vercel Cron job emails all 3 datasets to every super admin, unblocked by the Background Jobs decision above). Still unexercised against a real Vercel Cron deployment/Resend key, same standing caveat as Paystack.
- [x] Whether suspending a tutor (`super-admin/tutors`) should cascade to their courses — resolved 2026-08-08: yes, flags their `published` courses (reversible, AuditLog-tracked so reactivation never un-flags a moderator's own independent flag). See `docs/build-log.md`.
- [x] Paystack subaccount creation/onboarding flow for tutors — real, built 2026-08-08 (`app/tutor/settings`'s Payouts tab), including a disconnect/reconfigure action added the same day. Still unexercised against a real Paystack account (no test keys in this environment) — see `docs/build-log.md`'s 2026-08-08 entries.
- [x] Anti-cheat / exam-session integrity — tab-switch logging (real, "just log it," threshold 3) + single-attempt enforcement (`allowRetake`/`maxAttempts`, server-side) both real. **Audited 2026-08-18** (full before/during/after breakdown, web + mobile): confirmed symmetric and real via the shared `lib/student/exam-attempt.ts`; the one gap found (flagged attempts invisible to school admins, only ever shown on `super-admin/live-activity`) closed the same day via `app/school-admin/flagged-attempts`, tenant-scoped to the admin's own school.
- [x] Exam program/track modeling, subscription pricing, class naming, School.type split, Payment/Invoice vocabulary, Topic entity, Assessment status enum, Achievement catalog, billing-page duplication, demo-data → seed-script consolidation, course-marketplace entity design — see [`docs/build-log.md`](./docs/build-log.md) for the resolution writeups.

## Notes for Claude Code

- This file is the primary source of *current-state* project context — more current than any chat summary. Append detailed "what was built and how it was verified" writeups to **[`docs/build-log.md`](./docs/build-log.md)**, not here; only touch this file's summary sections when the actual current state changes (a lane closes, an open decision resolves, a new system-wide gotcha is found).
- **[`PROGRESS.md`](./PROGRESS.md) is a user-facing, plain-language progress/launch-readiness tracker** — a different audience than this file (the user reading for status, not a future Claude session reading for implementation context). Add a dated entry to its Log section whenever a real feature ships or a real launch-blocker is resolved/discovered - keep entries short (1-2 lines, what shipped + what it unblocks), don't duplicate `docs/build-log.md`'s technical depth there.
- **[`PROGRESS.md`](./PROGRESS.md) is a user-facing, plain-language progress/launch-readiness tracker** — a different audience than this file (the user reading for status, not a future Claude session reading for implementation context). Add a dated entry to its Log section whenever a real feature ships or a real launch-blocker is resolved/discovered - keep entries short (1-2 lines, what shipped + what it unblocks), don't duplicate `docs/build-log.md`'s technical depth there.
- Always raise new system-design decisions (new dependency, new service, schema shape for a not-yet-modeled entity, business-rule numbers like fee %) with the user rather than deciding unilaterally.
- When implementing a backend feature, cross-check the relevant `app/**` page(s) first for the exact fields/statuses/permissions the UI already assumes.
- **The user runs multiple Claude Code sessions against this repo, deliberately and in parallel.** When assigning the other session a task, ALWAYS give it two ways: (a) recorded here in CLAUDE.md, AND (b) as literal copy-paste text in the chat response — CLAUDE.md alone is not a reliable inter-session channel. Never trust another session's "done" claim at face value — independently re-verify (typecheck + live DB/HTTP check) before building on top of it or assigning follow-on work in the same area.
- **Cross-check web/mobile parity on every UI/UX-facing fix, not just feature work (added 2026-08-15).** When you change something a user sees or interacts with on web (validation, required-field markers, link behavior, legal/consent copy, etc.), explicitly check whether the mobile app needs the same fix before considering the task done — don't assume "web-only" without verifying. Two outcomes are both legitimate: (a) mobile genuinely doesn't need it (e.g. it defers to the same web page/API rather than rendering its own copy — confirm this by reading the mobile code, not by assuming), or (b) it's a real gap, in which case fix it directly if it's small/low-risk (e.g. a label string) after confirming via `ListAgents` that the mobile session isn't mid-edit, or hand it off explicitly (both ways, per the rule above) if it's substantive Flutter/Dart work. Saying "noted for going forward" in a chat response does not persist anywhere on its own — if it needs to survive to the next session, it has to actually be written here.
- **All 5 role lanes (Content Admin, School Admin, Super Admin, Student+signup, Tutor) are closed as of 2026-08-08** — every page is either wired to the real DB or confirmed correctly static. `school-admin/subscription/*` was the one deliberate hold-back (pending a Paystack conversation) and is now done too. Before starting new work, check `docs/build-log.md`'s lane-closure notes and `git status`/`git log` so two sessions don't duplicate work — remaining work is marketplace follow-ups and the Open Decisions above, not page-wiring.
- **`npm run db:seed` is destructive (wipes every table) and dangerous with multiple sessions active.** Never run it or any schema-resetting command without confirming with the user first.
- **Done 2026-08-10**: `/terms` and `/privacy` now carry real, product-accurate drafted legal text (a new `components/legal-draft-notice.tsx`, distinct from `components/legal-placeholder-notice.tsx` which `/cookies` still correctly uses as a placeholder). Explicitly a first draft for the user/counsel to review, not final/lawyer-reviewed text — both pages keep a visible "pending legal review" banner. See `docs/build-log.md`'s 2026-08-10 entry for exactly what each page covers and how it was verified.
- **Shared infra files should not need touching for routine page-wiring**: `lib/prisma.ts`, `auth.ts`, `auth.config.ts`, `proxy.ts`, `prisma/schema.prisma`, `components/data-table.tsx`, `components/dashboard-shell.tsx`. If a task seems to require changing one, pause and confirm with the user first — a change here affects every other session's pages too. (`prisma/schema.prisma` has been extended additively several times with explicit sign-off — additive new models/fields are lower-risk than editing existing ones, but still confirm first.)
- **Non-interactive migration recipe** (since `prisma migrate dev` fails non-interactively in this sandbox): `npx prisma migrate diff --from-config-datasource prisma/schema.prisma --to-schema prisma/schema.prisma --script` → hand-place into `prisma/migrations/<UTC-timestamp>_<name>/migration.sql` (timestamp via `date -u +%Y%m%d%H%M%S`) → `npx prisma migrate deploy` → `npx prisma generate`. Used successfully 5 times; if a `P1002` advisory-lock timeout occurs, retry once a concurrent session's connection has had time to close — not a sign of corruption. **2026-08-16**: the CLI form of this recipe (unlike `lib/prisma.ts`/`prisma/seed.ts`, which already bake in the IPv6 workaround) can fail with a bare `P1001` "Can't reach database server" - prefix the command with `NODE_OPTIONS="--dns-result-order=ipv4first"` if that happens, same underlying no-IPv6-route cause as this file's Database section already documents.
- **This Next.js version's dev-server lock is per-project-directory, not per-port.** Starting a second `next dev` (yours or a concurrent session's) against the same directory gets silently refused — the log prints "Ready" but the port never actually accepts connections. Verify with `curl` before trusting the log line; if blocked, don't wait or fight the lock — fall back to direct-DB temp-script verification (this project's primary verification method already, works for everything except literally rendering a page through HTTP). Never kill another process with a broad `pkill -f "next dev"` — it can (and has) taken out a concurrent session's server too; kill only a PID you started.
- Since other sessions may be running concurrently, run `git status`/`git log` (or `git pull`) at the start of a task if picking up work that isn't fresh.
- **`.env` points at a real, live Neon database** (project `typ`) — gitignored, so this note is the only record other sessions on this machine have.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
